<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>File Details - Sky Cloud</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
</head>
<body class="bg-gray-900 text-gray-100 min-h-screen flex flex-col items-center p-6">
  <h1 class="text-3xl mb-6">File Details</h1>
  <div class="bg-gray-800 p-6 rounded-lg shadow-md w-full max-w-md">
    <p><strong>File Name:</strong> <%= file.originalName %></p>
    <p><strong>Size:</strong> <%= (file.size / 1024).toFixed(2) %> KB</p>
    <p><strong>Uploaded At:</strong> <%= new Date(file.uploadedAt).toLocaleString() %></p>
    <p><strong>Visibility:</strong> <%= file.visibility %></p>
    <div class="flex justify-between w-full mt-4 space-x-2">
      <!-- Download Button -->
      <a href="/dashboard/file/<%= file.id %>/download" 
        class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded text-white text-sm">
        Download
      </a>
    
      <!-- Toggle Share Form -->
      <form action="/dashboard/file/<%= file.id %>/toggle-share" method="POST">
        <button type="submit" 
          class="<%= file.shared ? 'bg-yellow-500 hover:bg-yellow-600' : 'bg-green-500 hover:bg-green-600' %> px-4 py-2 rounded text-white text-sm"
        >
          <%= file.shared ? 'Unshare' : 'Share' %>
        </button>
      </form>
    
      <!-- Delete Form -->
      <form action="/dashboard/file/<%= file.id %>/delete" method="POST" onsubmit="return confirm('Delete this file?');">
        <button type="submit" 
          class="bg-red-500 hover:bg-red-600 px-4 py-2 rounded text-white text-sm">
          Delete
        </button>
      </form>
    </div>
  
  </div>
  
  <a href="/dashboard" class="mt-6 text-blue-400 hover:underline">Back to Dashboard</a>
</body>
</html>
